# Diário — 2026-09-14

Ver `diario/README.md` para o formato e as regras. Resumo: uma entrada por
evento relevante, sempre acrescentando no fim, nunca editando o que outra
sessão escreveu.

## 22:10 — documents — decide monorepo e abre com fases 1 e 2

Decisão: o motor de leitura de documentos ("ERA READ") fica em
`documents/`, dentro deste monorepo, e não no repositório separado
`github.com/iafrotamacedo-cloud/era-read` (que existia de antes da fusão em
monorepo e continua vazio — candidato a arquivar).

Primeiros dois pacotes, sem rede nem modelo: `imgproc` (cinza, bilinear,
resize, normalizacao de iluminacao por blur de fundo com imagem integral)
e `geom` (Point, Polygon, Homography via DLT, PolyFit, Remap). Commits
2264207 e bbd754e, direto na main. Testes, `gofmt`, `go vet` e
`go list -m all` (zero deps) conferidos; `-race` não roda nesta máquina
(sem compilador C, é o de sempre).

Próximo: fase 3 (`detect`, DBNet) já entra em rede — vai precisar checar
`faces/graph` antes (ops que faltam: `ConvTranspose`, `LayerNormalization`,
`Erf` — não é trabalho desta sessão, é do `faces`).

## 23:05 — documents — adianta o medidor de deformação (fase 4, parcial)

Pacote `dewarp`: mede a curvatura de uma linha de texto a partir do
polígono que a delimita (convenção DBNet/TotalText: metade dos vértices é a
borda de cima esq→dir, a outra metade a de baixo dir→esq) e decide entre
N0/N1/N2/N3. Só depende de `geom`, por isso adiantou fora da ordem do
roteiro — sem a fase 3 (`detect`) ainda não existindo, os testes usam
polígono sintético, não detecção de verdade. Commit 2ca0e06.

Falta, ainda dentro da fase 4: a retificação por linha do N2 em si (este
commit só mede a deformação, não retifica). N1 já está coberto —
`geom.RemapHomography`, da fase 2, já faz a retificação inteira.

## 23:40 — documents — fecha a fase 4: retificação N2

`geom.RemapCurve`: amostra uma faixa da imagem seguindo uma curva, em
espaçamento igual de comprimento de arco (não de x) e ao longo da normal
local. `dewarp.RectifyLine` liga isso à baseline de uma linha (ajusta a
curva via `geom.PolyFit` e delega). Testado contra formula fechada (reta) e
com uma faixa clara sobre parábola que sai reta depois de retificar, do
jeito real (baseline com poucos pontos, PolyFit redescobrindo a curva —
não a curva exata passada a mao). Commit 22047b6.

Fase 4 fecha aqui — falta só N3, que precisa de rede (fora de escopo até a
fase 3 do `faces` liberar as ops que faltam, já registrado acima).

## 00:20 — documents — adianta fatias de layout (fase 6) e extract (fase 7)

`extract`: CNPJ/CPF com dígito verificador (algoritmo oficial da Receita) e
busca em texto livre — o próprio dígito filtra ruído de reconhecimento.
Data (numérica + por extenso) e valor monetário (`Money`, inteiro em
centavos, não float64). Não depende de nenhuma fase anterior — puro
string→dado. Commit f91d5d8.

`layout`: `GroupLines` agrupa palavras em linhas só por geometria
(sobreposição vertical) e ordena em ordem de leitura. Deliberadamente só
cobre uma coluna — múltiplas colunas/tabela ficaram de fora porque o
limiar de separação só dá para calibrar com documento real, que ainda não
existe. Commit b3bcb7b.

Achado no caminho: bug real de regex em `ParseMoney` — `\d{1,3}(?:\.\d{3})*`
com `*` casava só 3 dígitos de "1234" antes de checar se havia separador de
milhar depois (zero repetições do grupo opcional já contava como
casamento válido, e o motor de regex do Go não troca de alternativa atrás
de um casamento mais longo). Trocado para `+` (exige pelo menos um grupo).
Pego pelo próprio teste, não em produção — mas fica registrado porque é o
tipo de bug de regex que se repete fácil em qualquer parser de número
BR/EUA.

## 00:45 — documents — CI quebrou no macOS (ARM64), corrigido

O push anterior (8bf98cd) quebrou só `Testes / macos-latest` — Linux,
Windows, `-race`, vet, cross-compile (11 plataformas) continuaram verdes.
Não era bug de cálculo: `TestMeasureLineSoDoisPontosCaiParaReta` exigia
`LinearBow == 0` exato, mas o valor passa pela eliminação gaussiana de
`geom.PolyFit` antes de chegar no teste — no ARM64 do runner isso deixou um
resto de arredondamento de ~8 ULPs (1.78e-15) que no AMD64 cancelou para
0.0 bit a bit. Corrigido para tolerância (1e-9), igual ao resto do arquivo
já fazia. Commit 5e1a54f.

Vale para quem mexer em `documents` depois: **cuidado com igualdade exata
de float em teste**, mesmo quando o resultado matemático é zero de verdade
— só é seguro se não passar por nenhuma conta (early return, cópia direta
de variável, ou zero-value do Go). Se passou por soma/divisão/eliminação
gaussiana, usa tolerância. Vi com o próprio log do GitHub Actions (via
Claude in Chrome, logado como o usuário — a API sem auth só devolve "must
have admin rights" mesmo em repo público).

## 01:15 — documents — pesquisa modelo de detecção, ATUALIZA o pedido pro `faces`

Lendo o código-fonte do PaddleOCR (não supondo pela arquitetura típica do
DBNet): a FPN inteira já upsample com `F.upsample(mode="nearest")`, que em
ONNX é `Resize` — **já suportado** no `faces/graph`. Só o `DBHead` usa
`ConvTranspose` de verdade (duas camadas finais, kernel 2 stride 2).

**Isso corrige o que esta própria sessão registrou hoje mais cedo**
(entrada de ~22:10 acima): não são três ops que faltam no `faces/graph`
(`ConvTranspose`, `LayerNormalization`, `Erf`), é **uma só**:
`ConvTranspose`. As outras duas são do SVTR (fase 5 do `documents`, o
reconhecedor), não do detector — não bloqueiam a fase 3. Isso nunca tinha
sido pedido formalmente ao `faces`, só especulado aqui; registra-se a
correção antes que vire pedido.

Licença do PP-OCRv4 confirmada na fonte primária: Apache 2.0. Commit
4ef218b, detalhe em `documents/README.md`.

Se alguém da sessão `faces` for implementar `ConvTranspose` no `graph`: o
caso de uso real é kernel 2×2, stride 2 (upsample 2x aprendido) — não
precisa generalizar além disso ainda.

## 01:30 — documents — aviso direto pra sessão `faces`: pedido de op

**Sessão `faces`, se ler isto:** o `documents` vai precisar da op
`ConvTranspose` no `faces/graph` para destravar a fase 3 (detecção de
texto, modelo DBNet/PP-OCRv4). É a única op nova que falta — motivo e
fonte na entrada de 01:15 logo acima. Caso de uso real, se ajudar a
escopar a implementação: kernel 2×2, stride 2, sem padding — não precisa
cobrir o operador ONNX `ConvTranspose` inteiro de primeira.

Não é bloqueio urgente pra ninguém agora (o `documents` ainda nem tem o
`.onnx` do modelo baixado), mas fica registrado como próximo pedido real
de op vindo daqui.
