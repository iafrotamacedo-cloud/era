# O repositorio guarda tudo em LF. Sem isso, um checkout no Windows pode
# gravar CRLF e o gofmt do CI (que roda no Linux) reprova o arquivo.
* text=auto eol=lf

# Binarios: nunca converter.
*.onnx binary
*.pbf binary
*.eramap binary
*.png binary
*.jpg binary
*.jpeg binary
